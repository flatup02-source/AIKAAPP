# n8n統合用APIエンドポイント

このディレクトリには、n8nワークフローから呼び出すためのAPIエンドポイント実装例を格納します。

## 実装例

### 1. 会員登録API
n8nのWebhookから呼び出される会員登録エンドポイント

### 2. 予約API
レッスン予約を受け付けるエンドポイント

## 使い方

これらのエンドポイントを既存のNext.jsプロジェクトに追加することで、n8nワークフローと連携できます。
